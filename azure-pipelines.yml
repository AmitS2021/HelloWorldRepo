# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(Build.Repository.LocalPath)'
    includeRootFolder: false
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
    replaceExistingArchive: true
- task: S3Upload@1
  inputs:
    awsCredentials: 'AWS-test-connection'
    regionName: 'ap-south-1'
    bucketName: 'mytelmytestbucket'
    sourceFolder: '$(Build.ArtifactStagingDirectory)'
    globExpressions: '**'
    targetFolder: 'MyTelmyAppDeliveries'
- task: CodeDeployDeployApplication@1
  inputs:
    awsCredentials: 'AWS-test-connection'
    regionName: 'ap-south-1'
    applicationName: 'Telmy-Dev'
    deploymentGroupName: 'Telmy-Dev'
    deploymentRevisionSource: 's3'
    bucketName: 'mytelmytestbucket'
    bundleKey: 'MyTelmyAppDeliveries/'
    description: 'Telmy-Dev deployment'
